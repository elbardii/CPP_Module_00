<div dir="rtl">

# شرح تمرين ex00 - Megaphone 📣

## 👋 السلام عليكم يا بطل (ة)!
مرحباً بك فـ C++ Module 00! هاد التمرين هو البداية ديال الرحلة ديالك فواحد من أقوى لغات البرمجة فالعالم.
الفكرة ديال هاد التمرين بسيطة بزاف: بغينا نصاوبو برنامج كيتصرف بحال شي "بوق" (Megaphone).
إلا هضرتي بشوية (كتبتي حروف صغار)، هو غايغوت عليك (غايردهم حروف كبار MAJUSCULE).

---

## 🧐 شنو كيدير هاد الكود بالضبط؟
البرنامج كيقرا داكشي لي كتبتي ليه حدا سميتو فالتيرمينال، وكيعاود يكتبو ولكن كلو **CAPITAL LETTERS**.
إلا ما كتبتي والو، كيغوت عليك وكايقول: `* LOUD AND UNBEARABLE FEEDBACK NOISE *`.

---

## 🧠 اجي نفهمو الكود دقة دقة (Code Analysis)

### 1. `main(int argc, char **argv)`
هاد الجملة هي الساروت ديال البرامج فـ C/C++ لي كتاخد inputs من التيرمينال.
*   **`argc` (Argument Count):** هو شحال من كلمة كتبتي فالتيرمينال (بما فيها سمية البرنامج).
*   **`argv` (Argument Vector):** هي طابلو (Tableau) عامر بالكلمات لي كتبتي. `argv[0]` ديما هو سمية البرنامج، و `argv[1]` هو الكلمة اللولة لي زدتي، وهكذا.

### 2. علاش درنا `if (argc == 1)`؟
حيت إلا كان `argc` كيساوي 1، كيعني أنك كتبتي غير `./megaphone` وما زدتي والو موراها.
فهاد الحالة، البرنامج خاصو يطبع رسالة ديال الصداع (Feedback noise) كيفما مطلوب فالسوجي.

### 3. البوكل (Loops)
```cpp
for (int i = 1; i < argc; ++i)
```
بدينا `i` من 1 ماشي من 0، حيت كيفما قلنا `argv[0]` هو سمية البرنامج وما بغيناش نطبعوها. حنا بغينا غير داكشي لي كتبتي موراها.

الـ Loop الثانية:
```cpp
for (int j = 0; argv[i][j]; ++j)
```
هادي كتدوز على كل حرف فكل كلمة باش نحولوهم واحد بواحد.

### 4. التحويل لـ MAJUSCULE (`std::toupper`)
```cpp
std::cout << (char)std::toupper(argv[i][j]);
```
*   `std::toupper`: فانكسيون كاينا فالمكتبة `<cctype>`، كتاخد حرف وكتردو كبير (`a` -> `A`).
*   `(char)`: هاد الـ cast ضروري حيت `toupper` كترد لينا رقم (ASCII code)، وحنا بغينا نطبعوه كحرف.
*   `std::cout`: هي "طبع ليا فالشاشة" (Standard Output). فـ C كنا كنديرو `printf`، ولكن فـ C++ كنخدمو بـ `std::cout`.

---

## 🛠️ كيفاش تجرب البرنامج؟

1.  **الكومبيلاسين (Compilation):**
    أول حاجة خاصك ترجم الكود لبرنامج يقدر يخدم. فالترمينال كتب:
    ```bash
    make
    ```
    غادي يتكون عندك ملف سميتو `megaphone`.

2.  **التجربة (Execution):**

    *   **تجربة خاوية:**
        ```bash
        ./megaphone
        ```
        النتيجة: `* LOUD AND UNBEARABLE FEEDBACK NOISE *`

    *   **تجربة بكلمات:**
        ```bash
        ./megaphone "shhhhh... I think the students are asleep..."
        ```
        النتيجة: `SHHHHH... I THINK THE STUDENTS ARE ASLEEP...`

    *   **تجربة بزاف د الكلمات:**
        ```bash
        ./megaphone Damnit " ! " "Sorry students, I thought this thing was off."
        ```
        النتيجة: `DAMNIT ! SORRY STUDENTS, I THOUGHT THIS THING WAS OFF.`

---

## ⚠️ نصيحة مهمة
فـ C++98، مازال كنتعاملو بزاف بـ `char*` (C-style strings) خصوصاً فالمين (main)، ولكن من بعد غاتولي تخدم بـ `std::string` لي هي أسهل وأحسن. فهاد التمرين خدمنا بـ `char` حيت `argv` جاي هكاك من عند النظام.

بالتوفيق ليك فالتمرين الجاي! 💪

</div>
