<div dir="rtl">

# شرح تمرين ex01 - My Awesome PhoneBook 📖

## 👋 مقدمة: علاش هاد التمرين؟
فهاد التمرين غادي تدخل للعالم الحقيقي ديال C++، وغادي تكتشف القوة ديال **Classes** و **Objects**.
المطلوب منك تصاوب "كوناشة ديال النمر" (PhoneBook) ولكن إلكترونية. كتقبل حتى لـ 8 ديال الأشخاص، وإلا زدتي التاسع، كتمسح القديم وكتحط الجديد بلاصتو.

---

## 🏗️ الهندسة ديال البرنامج (Architecture)
البرنامج ديالنا مبني على جوج ديال الركائز (Classes):

### 1. `Contact` (الشخص)
هاد الكلاس كيمثل **شخص واحد**.
تخايلو بحال شي ورقة كتعمر فيها المعلومات ديال بنادم:
*   السمية (First Name)
*   الكنية (Last Name)
*   اللقب (Nickname)
*   نمرة التيليفون (Phone Number)
*   شي سر خطير (Darkest Secret) 🤫

### 2. `PhoneBook` (الكوناشة)
هاد الكلاس هو "المدير". هو اللي كيتحكم فالكونتاكتس.
شنو عندو؟
*   عندو **طابلو (Array)** فيه 8 ديال الـ Contacts.
*   عندو "اندكس" (Index) كيعرف فين غادي يحط الكونتاكت الجاي.

---

## 🧠 مفاهيم جديدة خاصك تفهمها (C++ Concepts)

### 🧱 1. Object-Oriented Programming (OOP)
فـ C كنا كنخدمو غير بالفانكسيونات (Functions). فـ C++ كنجمعو الـ Data (المعلومات) والـ Functions (الخدمة) فبلاصة وحدة سميتها **Class**.
*   **Encapsulation:** "الستر". شي حوايج كنخبيوهم (`private`) وشي حوايج كنخليو الناس يخدمو بيهم (`public`).

### 🔒 2. `private` vs `public`
فالكود ديالك غادي تلقى:
```cpp
class Contact {
private:
    std::string _firstName; // ممنوع شي حد يقيسها ديريكت
public:
    void setFirstName(std::string name); // هاكيفاش نعمروها
};
```
*   **Private:** بحال "جيبي الخاص". مايمكنش لشي حد يجي ويهز ليا فلوسي.
*   **Public:** بحال "الباب د الدار". أي واحد يقدر يدق ويدخل.
حنا كنخبيو المعلومات (بحال `_firstName`) فـ `private`، وكنعطيو للناس فانكسيونات (`setters` و `getters`) باش يتحكمو فيها بطريقة آمنة.

---

## 🕹️ كيفاش كيخدم البرنامج؟

البرنامج كيدور فواحد الحلقة (Loop) وكيتسناك تعطيه أمر:

### 1. `ADD` (زيد شي واحد)
ملي كتكتب `ADD`:
1.  البرنامج كيسولك على المعلومات وحدة بوحدة.
2.  كيشد ديك المعلومات وكيخزنها فـ `Contact`.
3.  كيحط هاد `Contact` فالطابلو `_contacts`.
    *   **ملاحظة ذكية:** الطابلو فيه غي 8 بلايص. إلا عمر، كنرجعو للأول (Index 0) وكنحطو فوق القديم. هادشي كنسميوه **Circular Buffer** وكنحسبوه بـ `% 8`.

### 2. `SEARCH` (قلب على شي واحد)
ملي كتكتب `SEARCH`:
1.  البرنامج كيعرض ليك جدول فيه كاع الناس لي مسجلين.
2.  كيستعمل `std::setw(10)` باش يقاد العبار ديال الأعمدة (كل عمود فيه 10 د الحروف).
3.  كيقلب على السمية، وإلا كانت طويلة، كيقطعها وكيزيد نقطة فالاخر (مثلاً `Abderrah...`).
4.  كيطلب منك تختار "Index" (الرقم الترتيبي).
5.  إلا اختاريتي رقم صحيح، كيعرض ليك المعلومات كاملة ديال داك السيد.

### 3. `EXIT` (خرج)
ساهلة ماهلة. البرنامج كيسالي، وكاع داكشي لي سجلتي **كيمشي** (حيت مسجل غير فالرام RAM).

---

## 🚫 أخطاء شائعة (رد بالك منها!)

*   **`std::cin` vs `std::getline`:**
    *   `cin >> str` كتحبس عند أول مسافة (Espace). يعني إلا كتبتي "Ismail Z", غاتشد غير "Ismail".
    *   `std::getline(std::cin, str)` كتشد السطر كامل. خدم بـ `getline` باش تهني راسك.

*   **EOF (Ctrl+D):**
    *   إلا وركتي على Ctrl+D فوسط الـ input، البرنامج خاصو يحبس وماشي يبقا يدور فالخوا (Infinite loop). ديما دير `if (!cin) break;` ولا `exit(0)`.

*   **Index الغالط:**
    عندك تمشي تدخل شي رقم كبر من الطابلو وتصدق فـ Segmentation Fault. ديما تأكد أن الرقم بين 0 و 7 (أو 1 و 8 على حساب العرض).

---

## 🎓 خلاصة
هاد التمرين كيعلمك التنظيم. عوض ما تلوح المتغيرات (Variables) فكل قنت، كتجمعهم فـ Classes.
الـ `Contact` كيجمع سميتو وكنيتو... والـ `PhoneBook` كيجمع الـ Contacts. هادا هو أساس البرمجة كائنية التوجه (OOP).

يلاه، تلاح للكود وجرب تخدمو:
```bash
make
./phonebook
```

</div>
